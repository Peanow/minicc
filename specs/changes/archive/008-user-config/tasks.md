# 008-user-config tasks

- [x] T1 [R1, AC1, AC2] Extend dotenv discovery and precedence with optional
  user-level configuration while preserving defaults and process overrides.
- [x] T2 [R2, AC3] Emit safe ``config_loaded`` startup Trace metadata before
  runtime validation and expose the typed event name.
- [x] T3 [R3, AC4] Add the private user-file initializer and unit tests for
  creation mode, supplementation, precedence, compatibility, and redaction.
- [x] T4 [R1, R2, R3, AC1, AC2, AC3, AC4] Update runtime capability guidance
  and complete the verification suite.
