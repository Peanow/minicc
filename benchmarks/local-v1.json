{
  "schema_version": 1,
  "name": "local-v1",
  "tier": "tier1",
  "repetitions": 3,
  "models": [
    {
      "id": "deepseek-v4-flash",
      "model": "deepseek-v4-flash",
      "base_url": "https://api.deepseek.com",
      "api_key_env": "OPENAI_API_KEY",
      "enabled": true
    },
    {
      "id": "comparison-model",
      "model": "replace-with-an-openai-compatible-model-id",
      "api_key_env": "CORECODER_EVAL_COMPARISON_API_KEY",
      "enabled": false
    }
  ],
  "strategies": [
    {
      "id": "hybrid-workspace",
      "context_strategy": "hybrid",
      "permission_mode": "workspace-write",
      "tokenizer": "auto",
      "max_context_tokens": 12000,
      "enabled": true
    },
    {
      "id": "truncate-workspace",
      "context_strategy": "truncate",
      "permission_mode": "workspace-write",
      "tokenizer": "auto",
      "max_context_tokens": 12000,
      "enabled": true
    },
    {
      "id": "summary-workspace",
      "context_strategy": "summary",
      "permission_mode": "workspace-write",
      "tokenizer": "auto",
      "max_context_tokens": 12000,
      "enabled": true
    }
  ],
  "tasks": [
    {
      "id": "python-inclusive-range",
      "fixture": "tasks/python-inclusive-range",
      "prompt": "Fix inclusive_sum in range_utils.py. It must include both endpoints and raise ValueError when start is greater than end. Keep the public function signature unchanged. Run the provided verifier before finishing.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "hidden_checks": [
        {"path": "hidden/local-v1/python-inclusive-range.py"}
      ],
      "expected_change_paths": ["range_utils.py"],
      "tags": ["python", "bugfix", "boundary"]
    },
    {
      "id": "python-record-normalizer",
      "fixture": "tasks/python-record-normalizer",
      "prompt": "Complete normalize_records in normalizer.py. Ignore blank and comment lines, trim each comma-separated field, reject malformed rows with ValueError including the 1-based line number, and return dictionaries with name and role keys. Keep the implementation dependency-free and run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["normalizer.py"],
      "tags": ["python", "feature", "parsing"]
    },
    {
      "id": "python-ttl-cache",
      "fixture": "tasks/python-ttl-cache",
      "prompt": "Implement expiration in TTLCache without changing its constructor or method signatures. Use the injected clock, refresh a key's deadline when set is called again, treat ttl_seconds=0 as immediately expired, and remove expired entries when read. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["cache.py"],
      "tags": ["python", "state", "time"]
    },
    {
      "id": "python-safe-path",
      "fixture": "tasks/python-safe-path",
      "prompt": "Harden resolve_member in archive.py against archive path traversal. It must return the resolved destination for safe relative members and raise ValueError for absolute paths, parent traversal, or prefix-confusion escapes. Do not access the filesystem beyond path resolution. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "hidden_checks": [
        {"path": "hidden/local-v1/python-safe-path.py"}
      ],
      "expected_change_paths": ["archive.py"],
      "tags": ["python", "security", "paths"]
    },
    {
      "id": "python-multifile-slug",
      "fixture": "tasks/python-multifile-slug",
      "prompt": "Add a dependency-free slugify function in catalog/text.py and make build_entry in catalog/entries.py use it. Slugs must be lowercase ASCII, collapse runs of non-alphanumeric characters to one hyphen, and never start or end with a hyphen. Preserve build_entry's return shape and run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["catalog/text.py", "catalog/entries.py"],
      "tags": ["python", "multi-file", "feature"]
    },
    {
      "id": "python-scoped-instructions",
      "fixture": "tasks/python-scoped-instructions",
      "prompt": "Fix src/reporting.py so summarize returns the required aggregate for empty and non-empty inputs. Respect all project instructions, keep its public signature stable, and run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["src/reporting.py"],
      "tags": ["python", "instructions", "aggregation"]
    },
    {
      "id": "python-retry-policy",
      "fixture": "tasks/python-retry-policy",
      "prompt": "Implement run_with_retry in retry.py without changing its signature. Attempt at most max_attempts times, retry only exceptions accepted by should_retry, call sleeper between retries but never after the final attempt, and re-raise the original exception when stopping. Reject max_attempts below 1 with ValueError. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "hidden_checks": [
        {"path": "hidden/local-v1/python-retry-policy.py"}
      ],
      "expected_change_paths": ["retry.py"],
      "tags": ["python", "reliability", "retry"]
    },
    {
      "id": "python-deep-merge",
      "fixture": "tasks/python-deep-merge",
      "prompt": "Implement deep_merge in merge.py. Recursively merge dictionaries, replace non-dictionary values including lists, preserve keys from both sides, and do not mutate either input. Keep the signature and use only the standard library. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["merge.py"],
      "tags": ["python", "configuration", "immutability"]
    },
    {
      "id": "python-topological-order",
      "fixture": "tasks/python-topological-order",
      "prompt": "Implement dependency_order in graph.py. Return a deterministic topological order using lexical order when multiple nodes are ready, include dependencies that appear only in value lists, and raise ValueError containing 'cycle' for cyclic graphs. Do not mutate the input. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["graph.py"],
      "tags": ["python", "algorithm", "dependencies"]
    },
    {
      "id": "python-secret-redaction",
      "fixture": "tasks/python-secret-redaction",
      "prompt": "Complete redact_secrets in redaction.py. Replace values in Bearer authorization tokens and api_key/token/password key-value pairs with [REDACTED], case-insensitively, while preserving surrounding text and avoiding changes to ordinary words. Do not add dependencies. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["redaction.py"],
      "tags": ["python", "security", "observability"]
    },
    {
      "id": "python-lazy-batches",
      "fixture": "tasks/python-lazy-batches",
      "prompt": "Implement iter_batches in batches.py as a lazy iterator over any iterable. Preserve order, emit a final partial batch, consume the source only as needed, and raise ValueError for size below 1. Keep the signature and run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["batches.py"],
      "tags": ["python", "iterators", "data-processing"]
    },
    {
      "id": "python-cursor-pagination",
      "fixture": "tasks/python-cursor-pagination",
      "prompt": "Implement collect_pages in pagination.py. Call fetch_page(cursor) starting with None, concatenate items in order, stop when next_cursor is None, and raise ValueError containing 'cursor' if a cursor repeats. Keep the signature and run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["pagination.py"],
      "tags": ["python", "api", "pagination"]
    },
    {
      "id": "python-config-precedence",
      "fixture": "tasks/python-config-precedence",
      "prompt": "Implement resolve_config in config_layers.py. Merge defaults, file values, environment values, and CLI values in that precedence order; a value of None means 'not supplied' and must not overwrite a lower layer. Return a new dictionary and never mutate inputs. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["config_layers.py"],
      "tags": ["python", "configuration", "precedence"]
    },
    {
      "id": "python-event-deduplication",
      "fixture": "tasks/python-event-deduplication",
      "prompt": "Implement deduplicate_events in events.py. Keep one event per id using the greatest timestamp; when timestamps tie, the later input wins. Return events ordered by each id's first appearance, do not mutate inputs, and raise ValueError when an event lacks id or timestamp. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["events.py"],
      "tags": ["python", "events", "data-processing"]
    },
    {
      "id": "python-sse-parser",
      "fixture": "tasks/python-sse-parser",
      "prompt": "Implement parse_sse in sse.py for an iterable of text lines. Ignore comment lines, collect one or more data: lines until a blank separator, join multiple data values with a newline, emit the final pending event at EOF, and stop without emitting when data is [DONE]. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["sse.py"],
      "tags": ["python", "streaming", "protocol"]
    },
    {
      "id": "python-tool-arguments",
      "fixture": "tasks/python-tool-arguments",
      "prompt": "Implement validate_arguments in tool_schema.py for object-style tool schemas. Report missing required fields, unexpected fields when additionalProperties is false, and type mismatches for string, integer, number, boolean, array, and object. Treat bool as distinct from integer/number. Return a stable list of human-readable errors and do not mutate inputs. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["tool_schema.py"],
      "tags": ["python", "tools", "validation"]
    },
    {
      "id": "python-message-budget",
      "fixture": "tasks/python-message-budget",
      "prompt": "Implement select_messages in budget.py. Keep the first system message when present, then retain the largest contiguous suffix of remaining messages whose counter costs fit the remaining budget. Return new message dictionaries, reject negative budgets, and do not mutate inputs. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["budget.py"],
      "tags": ["python", "context", "token-budget"]
    },
    {
      "id": "python-circuit-breaker",
      "fixture": "tasks/python-circuit-breaker",
      "prompt": "Complete CircuitBreaker in circuit.py without changing its public API. Open after failure_threshold consecutive failures, raise CircuitOpenError without calling the operation during the cooldown, allow one half-open trial after recovery_seconds, reset on success, and reopen on a failed trial. Use the injected clock. Run verify.py.",
      "checks": [
        {
          "argv": ["python", "verify.py"]
        }
      ],
      "protected_paths": ["verify.py"],
      "expected_change_paths": ["circuit.py"],
      "tags": ["python", "reliability", "state-machine"]
    }
  ]
}
